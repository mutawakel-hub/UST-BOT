# 📋 سجل التغييرات — UST Central Bot

> هذا السجل يوثّق التطوّر من الإصدار الأولي (v1.0) حتى الإصدارات الحالية:
> **بوت الطالب v3.0** (متكامل مع Supabase) · **بوت الإدارة v3.3** (تكامل جزئي).

---

## v3.3 (2026-08-XX) — بوت الإدارة: تكامل جزئي مع Supabase

### ✨ ميزات جديدة
- ✅ **ربط المساهمات بـ Supabase** — تُقرأ من جدول `contributions` وتُحفظ القرارات (اعتماد/اعتماد مميز/رفض) فيها
- ✅ **ربط التعميمات بـ Supabase** — تُسجَّل كل التعميمات في جدول `broadcasts` مع `sent_count` و `status`
- ✅ **تعميمات ديناميكية حسب RBAC** — يحدّد النطاق (الكل/كلية/تخصص/مستوى) المستلمين عبر Function `get_broadcast_recipients(...)`
- ✅ **معاينة قبل التعميم** — يظهر عدد المستلمين المتوقَّع قبل التأكيد
- ✅ **نظام التكريم** — صلاحيات جديدة: `manage_honors`, `reset_points`, `view_honors_log`

### 🗄️ قاعدة البيانات
- ✅ إضافة جدول `student_points` — تتبّع نقاط الطلاب
- ✅ إضافة جدول `contribution_honors` — تكريمات المساهمات
- ✅ إضافة جدول `points_reset_logs` — سجل عمليات تصفير النقاط
- ✅ إضافة جدول `student_notifications` — إشعارات الطلاب
- ✅ إضافة جدول `student_subscriptions` — اشتراكات الطلاب في المواد
- ✅ إضافة 3 Functions: `get_top_contributors_specialty`, `award_contribution_points`, `notify_contribution_rejected`
- ✅ إضافة 3 صلاحيات للتكريم والنقاط

### 📊 إحصائيات
- عدد الجداول: **24**
- عدد الـ Functions: **6**
- عدد الصلاحيات: **19**
- عدد المناصب: **9**

---

## v3.0 (2026-08-XX) — بوت الطالب: تكامل كامل مع Supabase (Production-Ready)

### 🎉 نقلة نوعية
- ❌ **إزالة كل البيانات الوهمية** من بوت الطالب — كل البيانات الآن من Supabase
- ✅ **تكامل كامل مع Supabase** عبر `src/shared/db.ts` (عميل PostgREST خفيف)
- ✅ **نظام تسجيل صريح** عند أول `/start` — الطالب يختار الكلية، التخصص، المستوى
- ✅ بوت الطالب أصبح **جاهزاً للإنتاج**

### ✨ ميزات جديدة
- ✅ **شاشة تسجيل جديدة** — عند أول `/start`، يُطلَب من الطالب تعبئة بياناته
- ✅ **استرجاع المواد** من جدول `subjects` حسب تخصص الطالب ومستواه
- ✅ **استرجاع المحتوى** من جدول `content` مع `telegram_file_id` لإرسال الملف الفعلي
- ✅ **شاشة معاينة الملف** تُظهر البيانات الحقيقية من Supabase (حجم، رافع، تاريخ، عدّاد)
- ✅ **إرسال الملف الفعلي** من قنوات التخزين (`forwardMessage` أو `copyMessage`)
- ✅ **لوحة الشرف الحقيقية** — استعلام من `students` مرتّب حسب `total_points`
- ✅ **حسابي** — إحصائيات حقيقية + مساهماتي + تحميلاتي + إشعاراتي
- ✅ **تسجيل التحميلات** في جدول `downloads`
- ✅ **تسجيل المساهمات** في جدول `contributions`

### 🔗 قنوات التخزين الفعلية
- ✅ ربط كلية الطب بقناة تخزين: `-1004405014472`
- ✅ ربط كلية الحاسبات بقناة تخزين: `-1003727164402`
- ✅ تخزين `storage_channel_id` في جدول `colleges`

### 🗄️ قاعدة البيانات
- ✅ إضافة Function `register_student(...)` — تسجيل الطالب مع `ON CONFLICT DO UPDATE`
- ✅ إضافة Function `get_broadcast_recipients(...)` — استرجاع مستلمي التعميم
- ✅ إضافة View `user_permissions` — يجمع كل صلاحيات المستخدم من كل مناصبه

### 🗑 محذوفات
- ❌ حذف كل الدوال الوهمية (`getLeaderboard`, `getSubjects` وغيرها)
- ❌ حذف كل النصوص التي تشير إلى "وهمي" أو "تجريبي" في بوت الطالب
- ❌ حذف استيرادات البيانات الثابتة من `src/shared/data/*` في بوت الطالب (تحوّلت لاستعلامات Supabase)

---

## v2.2 (2026-07-27)

### 🗑 محذوفات
- ❌ حذف ميزة **الاشتراك في المواد** بالكامل من البوت (بناءً على طلب العميل)
  - حذف زر "🔔 اشتراك" / "🔕 إلغاء الاشتراك" من شاشة قائمة المادة (S7)
  - حذف زر "🔔 اشتراكاتي" من شاشة الحساب (S12)
  - حذف شاشة "اشتراكاتي" بالكامل
  - حذف حقل `subscriptions_count` من إحصائيات الحساب
  - حذف `subscriptions: Set<number>` من UserState
  - حذف handlers: `subscribe_*`, `unsubscribe_*`, `my_subscriptions`
  - حذف `mySubscriptionsKeyboard` من keyboards.ts
  - تنظيف كل النصوص المرتبطة من texts.ts

> **ملاحظة v3.x:** عاد نظام الاشتراكات لاحقاً في v3.3 عبر جدول `student_subscriptions` (لكن بقاعدة بيانات حقيقية).

### ✨ تأثير
- شاشة قائمة المادة أصبحت أنظف (4 أزرار فقط بدل 6)
- شاشة الحساب أنظف (3 أزرار بدل 4)
- تقليل حجم الكود بحوالي 100 سطر
- تبسيط تجربة المستخدم

---

## v2.1 (2026-07-26)

### 🐛 إصلاحات
- ✅ إصلاح مشكلة الأخطاء المتكررة في الـ Webhooks (لا مزيد من الأخطاء 500)
- ✅ إضافة `bot.catch` شامل يتجاهل الأخطاء القابلة للتجاهل (query is too old, message not modified, ...)
- ✅ تحويل أخطاء الـ webhook إلى استجابة 200 (لمنع إعادة المحاولة من Telegram)
- ✅ تنظيف التحديثات المعلّقة في بوت الطالب (كانت 4، الآن 0)

### ✨ تحسينات
- ✅ إضافة سكريبت `webhook-manager.js` لإدارة الـ webhooks (status/clear/reset)
- ✅ إضافة سكريبت `setup.js` للإعداد الكامل بضغطة واحدة
- ✅ دمج PDF Server داخل المشروع الرئيسي (مجلد `pdf-server/`)
- ✅ إضافة `workers_dev = true` صراحةً في wrangler.toml
- ✅ تحديث `.gitignore` ليشمل كل الملفات الحساسة

### 📚 توثيق
- ✅ README.md شامل (دليل التثبيت، النشر، استكشاف الأخطاء)
- ✅ `.env.example` بمتغيرات كاملة
- ✅ `.dev.vars.example` للتطوير المحلي

---

## v2.0 (2026-07-26)

### ✨ ميزات جديدة كبرى
- ✅ **شاشة معاينة الملف** قبل التحميل (اسم، حجم، تاريخ، رافع، عدّاد)
- ✅ **إرسال ملف PDF فعلي** عند التحميل (عبر PDF Server Worker)
- ✅ **Breadcrumb** في كل شاشة (مسار التنقّل)
- ✅ **نظام تسجيل دخول** للإدارة (4 معرّفات تجريبية بأدوار مختلفة)
- ✅ **تأكيد قبل الرفض** (شاشة منفصلة)
- ✅ **شريط تقدّم** في معالج رفع الملفات
- ✅ **عدّاد ملفات** بجانب كل تصنيف
- ✅ **بحث شامل** في المواد والملفات
- ✅ **لوحة شرف** بـ 10 طلاب + تصفية بالكلية/التخصص

### 🎨 تحسينات تجربة المستخدم
- ✅ إيموجي لكل كلية (🏥 🦷 💊 ⚙️ 💻 📊 📚)
- ✅ تصنيفات ملوّنة بالإيموجي (📘 📗 📑 📝)
- ✅ بيانات ثابتة لكل مستخدم (بدل Math.random)
- ✅ تنقّل موحّد (editMessageText بدل reply)

### 📦 بيانات جديدة
- ✅ 7 كليات (كاملة)
- ✅ 34 تخصصاً (كاملة)
- ✅ 20 مادة فعلية في تخصص IT
- ✅ 10 طلاب في لوحة الشرف
- ✅ 4 مسؤولين تجريبيين
- ✅ 5 مساهمات معلّقة

---

## v1.0 (2026-07-26) — الإصدار الأولي

### ✨ الإطلاق
- ✅ بوت الطالب (12 شاشة)
- ✅ بوت الإدارة (15 شاشة)
- ✅ نشر على Cloudflare Workers
- ✅ تسجيل Webhooks مع Telegram
- ✅ بيانات وهمية أساسية (7 كليات + 28 تخصص)
